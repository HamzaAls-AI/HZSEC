{
  "name": "hzsec-backend",
  "version": "0.1.0",
  "private": true,
  "description": "HZSec backend — license/auth/billing/Anthropic-proxy service.",
  "main": "src/index.js",
  "type": "commonjs",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "node --watch src/index.js",
    "start": "node src/index.js",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev",
    "prisma:deploy": "prisma migrate deploy",
    "test": "echo \"no tests yet\" && exit 0"
  },
  "dependencies": {
    "@clerk/express": "^1.7.79",
    "@prisma/client": "^5.18.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "helmet": "^7.1.0",
    "morgan": "^1.10.0",
    "stripe": "^16.6.0"
  },
  "devDependencies": {
    "prisma": "^5.18.0"
  }
}
